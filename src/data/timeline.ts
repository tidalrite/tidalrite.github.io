import type { TimelineItem } from "../components/features/timeline/types";

// Add personal milestones here. The page remains available while this list is empty.
export const timelineData: TimelineItem[] = [];
